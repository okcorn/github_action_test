name: Check types
on: pull_request

jobs:
  check-types:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run type check lines
        run: yarn tsc --noEmit
